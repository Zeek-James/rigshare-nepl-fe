{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "apps/operators/build"
      }
    }
  ],
  "buildCommand": "CI=false npm run build:operators",
  "outputDirectory": "apps/operators/build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}